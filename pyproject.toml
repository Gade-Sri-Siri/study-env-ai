[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "study-env-ai"
version = "0.1.0"
description = "RL Environment for Meta-PyTorch Hackathon"
dependencies = [
    "fastapi",
    "uvicorn",
    "torch",
    "gymnasium",
    "numpy"
]
