version: "1.0"
name: "your-environment-name"  # e.g., traffic-control-env
entrypoint: "python3 inference.py"
type: "gymnasium"             # OpenEnv uses Gymnasium-style APIs
metadata:
  task: "short description of your RL task"
  render_modes: ["rgb_array", "human"]
